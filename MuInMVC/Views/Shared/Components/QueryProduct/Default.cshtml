@using MuInShared.Helpers
@model ProductQueryObject


<form class="form" asp-action="Filter" method="post">
	<div class="card-body">
		<div class="form-group">
			<label class="col-form-label text-right font-weight-bold">Nhãn hàng</label>
			<select class="form-control selectpicker custom-select" asp-for="BrandAlias" asp-items="ViewBag.BrandList">
				<option value="hhg" selected>Choose Color</option>
			</select>
		</div>
	</div>
	<div class="card-body">
		<div class="form-group">
			<label class="col-form-label text-right font-weight-bold">Đối tượng</label>
			<select class="form-control selectpicker custom-select" asp-for="@Model.FeatureAlias" asp-items="ViewBag.FeatureList">
				<option value="" selected>Choose Color</option>
			</select>
		</div>
	</div>
	@* <div class="card-body">
		<div class="form-group">
			<label class="col-form-label text-right font-weight-bold">Giá</label>
			<select class="form-control selectpicker custom-select">
				<option>Mustard</option>
				<option>Ketchup</option>
				<option>Relish</option>
			</select>
		</div>
	</div> *@
	<div class="card-body">
		<div class="form-group">
			<button type="submit" class="p-0 border-0 bg-white mb-5 btn shadow-none">
				<a class="cart-btn mb-0"><i class="fas fa-filter"></i> Lọc</a>
			</button>
		</div>
	</div>
</form>

